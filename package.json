{
  "name": "agent-army-system",
  "version": "1.0.0",
  "description": "Modular AI agent platform with foundational base and specialized agents",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspace=packages/worker",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "deploy": "npm run deploy --workspace=packages/worker"
  },
  "keywords": [
    "ai",
    "agent",
    "chatbot",
    "cloudflare",
    "workers"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "vitest": "^1.0.4"
  }
}

